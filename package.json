{
  "name": "speed-dial-quantum",
  "description":
    "Minimal speed-dial extension with support for keyboard shortcuts & importing Speed Dial pages",
  "version": "0.0.12",
  "private": true,
  "repository": "https://github.com/Ahimta/speed-dial-quantum",
  "author": "Abdullah Alansari",
  "license": "GPL-3.0-only",
  "scripts": {
    "build": "web-ext build",
    "build:js": "rollup --config",
    "start": "web-ext run",
    "test": "npm run lint",
    "lint": "npm run prettier && npm run standard && web-ext lint",
    "prettier":
      "prettier --write packages.json README.md rollup.config.js web-ext-config.js src/*.js",
    "standard": "standard --fix rollup.config.js src/*.js"
  },
  "devDependencies": {
    "prettier": "^1.11.1",
    "rollup": "^0.57.1",
    "standard": "^11.0.0",
    "web-ext": "^2.4.0"
  },
  "standard": {
    "globals": ["browser", "chrome"]
  }
}
